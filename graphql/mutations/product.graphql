extend type Mutation {
    createProduct(
        name: String!,
        price: Float!,
        description: String!
    ): Product!

    createProduct(
        input: CreateProductInput! @spread
    ): Product! @create
}
